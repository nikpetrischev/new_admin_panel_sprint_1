[project]
name = "new-admin-panel-sprint-1"
version = "0.1.0"
description = ""
authors = [
    {name = "Nikolai Petrishchev"}
]
readme = "README.md"
requires-python = ">=3.12,<4.0"
dependencies = [
    "django (==4.2.11)",
    "flake8 (==6.1.0)",
    "python-dotenv (>=1.2.2,<2.0.0)",
    "django-split-settings (>=1.3.2,<2.0.0)",
    "psycopg (>=3.3.3,<4.0.0)",
    "flake8-django (>=1.4,<2.0)"
]

[tool.poetry]
packages = [
    { include = "movies_admin" },
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
